min_maki_version = "0.5.3"

# fs_read and fs_write install the provider script into the config directory,
# run marks it executable, keeps the request proxy alive, and calls it for
# /antigravity.
[permissions]
fs_read = true
fs_write = true
run = true
